"""Top-level package for witty source modules.

This package contains the core modules for the Witty project. The
`src.types` module defines the Pydantic models used across the pipeline.

Keep this module minimal — it only exists so `src` is a proper Python
package and tests/imports work reliably.
"""

